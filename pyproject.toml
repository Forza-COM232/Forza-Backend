[project]
name = "forza-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "chayen", email = "romory@pop-os.CG-24D" }
]
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "httpx2>=2.12.0",
    "psycopg2-binary>=2.9.12",
    "pytest>=9.1.1",
    "sqlalchemy>=2.0.52",
]

[project.scripts]
forza-backend = "forza_backend:main"

[build-system]
requires = ["uv_build>=0.12.8,<0.13.0"]
build-backend = "uv_build"
